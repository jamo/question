= form_for @checkbox_option do |f|
  - if @checkbox_option.errors.any?
    #error_explanation
      %h2= "#{pluralize(@checkbox_option.errors.count, "error")} prohibited this checkbox_option from being saved:"
      %ul
        - @checkbox_option.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :title
    = f.text_field :title
  .actions
    = f.submit 'Save'
