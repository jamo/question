= form_for @checkbox_group do |f|
  - if @checkbox_group.errors.any?
    #error_explanation
      %h2= "#{pluralize(@checkbox_group.errors.count, "error")} prohibited this checkbox_group from being saved:"
      %ul
        - @checkbox_group.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :title
    = f.text_field :title
  .actions
    = f.submit 'Save'
